jobs:
  - job: Dotfiles
    timeoutInMinutes: 200
    pool:
      vmImage: 'win1803'
    steps:
      - powershell: .\src\scripts\init.ps1
        displayName: Run `init.ps1`
        env:
          CI: 'true'